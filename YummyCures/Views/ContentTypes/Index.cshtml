@model IEnumerable<YummyCures.Models.ContentType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ContentTypeDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ContentTypeDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ContentTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ContentTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ContentTypeID })
        </td>
    </tr>
}

</table>
